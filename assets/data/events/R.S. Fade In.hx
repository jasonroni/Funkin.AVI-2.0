function eventTrigger(params)
{
  FlxTween.tween(PlayState.opponent, {alpha: 1}, 10); // idk if it'll work but only one way to find out
}

function returnDescription()
	return
          "Fades R.S. in (Used only for Scrapped)";
